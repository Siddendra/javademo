package com.sidd.javademo.application.programs;/*
package com.sample.programs;

import java.util.HashMap;
import java.util.Map;

public class Sample {

    public static void main(String args[]) {
        Map<String,String> features = new HashMap<String, String>();
        features.put("phone_cap_display_name", "XYZ");
        features.put("seq_no", "0");
        features.put("ABC", "123");
        features.forEach(feature -> {
            if(feature.get("phone_cap_display_name").equalsIgnoreCase("XYZ".toUpperCase())) {
                feature.put("special_offers", "phoneCapDisplayName");
            }
        });
    }
}
*/
